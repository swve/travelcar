{% extends 'base_back.html.twig' %} {% block title %}Placetype{% endblock %} {%
block body %}
<h1>Placetype</h1>

<table class="table">
  <tbody>
    <tr>
      <th>Id</th>
      <td>{{ placetype.id }}</td>
    </tr>
    <tr>
      <th>Title</th>
      <td>{{ placetype.title }}</td>
    </tr>
  </tbody>
</table>

<a href="{{ path('placetype_index') }}">back to list</a>

<a href="{{ path('placetype_edit', { id: placetype.id }) }}">edit</a>

{{ include("placetype/_delete_form.html.twig") }}
{% endblock %}
