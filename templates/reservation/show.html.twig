{% extends 'base_back.html.twig' %} {% block title %}Reservation{% endblock %}
{% block body %}
<h1>Reservation</h1>

<table class="table">
  <tbody>
    <tr>
      <th>Id</th>
      <td>{{ reservation.id }}</td>
    </tr>
    <tr>
      <th>Date_start</th>
      <td>
        {{ reservation.dateStart ? reservation.dateStart|date('Y-m-d') : '' }}
      </td>
    </tr>
    <tr>
      <th>Date_end</th>
      <td>
        {{ reservation.dateEnd ? reservation.dateEnd|date('Y-m-d') : '' }}
      </td>
    </tr>
    <tr>
      <th>Time_start</th>
      <td>
        {{ reservation.timeStart ? reservation.timeStart|date('H:i:s') : '' }}
      </td>
    </tr>
    <tr>
      <th>Time_end</th>
      <td>
        {{ reservation.timeEnd ? reservation.timeEnd|date('H:i:s') : '' }}
      </td>
    </tr>
  </tbody>
</table>

<a href="{{ path('reservation_index') }}">back to list</a>

<a href="{{ path('reservation_edit', { id: reservation.id }) }}">edit</a>

{{ include("reservation/_delete_form.html.twig") }}
{% endblock %}
