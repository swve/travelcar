{% extends 'base.html.twig' %} {% block title %}Cars{% endblock %} {% block
body %}
<h1>Cars</h1>

<table class="table">
  <tbody>
    <tr>
      <th>Id</th>
      <td>{{ car.id }}</td>
    </tr>
    <tr>
      <th>Label</th>
      <td>{{ car.label }}</td>
    </tr>
    <tr>
      <th>Ref</th>
      <td>{{ car.ref }}</td>
    </tr>
  </tbody>
</table>

<a href="{{ path('my_cars_index') }}">< retour à la liste</a><br>

<a href="{{ path('my_cars_edit', { id: car.id }) }}" class="btn btn-outline-warning">Modifier</a>

{{ include("my_cars/_delete_form.html.twig") }}
{% endblock %}
