{% extends 'base_back.html.twig' %} {% block title %}Place{% endblock %} {%
block body %}
<h1>Place</h1>

<table class="table">
  <tbody>
    <tr>
      <th>Id</th>
      <td>{{ place.id }}</td>
    </tr>
    <tr>
      <th>Title</th>
      <td>{{ place.title }}</td>
    </tr>
    <tr>
      <th>City</th>
      <td>{{ place.city }}</td>
    </tr>
    <tr>
      <th>Capacity</th>
      <td>{{ place.capacity }}</td>
    </tr>
    <tr>
      <th>Address</th>
      <td>{{ place.address }}</td>
    </tr>
    <tr>
      <th>Position</th>
      <td>{{ place.position }}</td>
    </tr>
  </tbody>
</table>

<a href="{{ path('place_index') }}">back to list</a>

<a href="{{ path('place_edit', { id: place.id }) }}">edit</a>

{{ include("place/_delete_form.html.twig") }}
{% endblock %}
